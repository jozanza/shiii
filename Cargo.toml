[package]
name = "shiii"
version = "0.1.0"
edition = "2021"

[dependencies]
turbo = { version = "4.0.0-alpha.0", package = "turbo-genesis-sdk" }

[lib]
crate-type = ["cdylib"]

[lints.rust]
unexpected_cfgs = { level = "allow" }
